# Copy to .env and adjust. Every variable has a default in docker-compose.yml,
# so the stack also starts without a .env file.
POSTGRES_USER=approval
POSTGRES_PASSWORD=change-me
POSTGRES_DB=approval
CLIENT_PORT=8080
LOG_LEVEL=info
